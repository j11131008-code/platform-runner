import kaboom from "kaboom";

// 初始化 Kaboom 遊戲視窗
kaboom({
    background: [135, 206, 235], // 設定背景顏色為天空藍
    width: 800,
    height: 600,
    scale: 1,
});

// 載入你在 Replit 設計的遊戲場景與邏輯（這會自動去載入 App.tsx 或相關邏輯）
import "./App";
